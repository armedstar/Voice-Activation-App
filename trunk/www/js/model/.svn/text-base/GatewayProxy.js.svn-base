
GatewayProxy.prototype = new puremvc.Proxy;
GatewayProxy.NAME = "GatewayProxy";

function GatewayProxy()
{
    puremvc.Proxy.apply(this, [GatewayProxy.NAME, new Array()]);
    
    //if (this.gatewayService == null)
        //this.gatewayService = new window.plugins.GatewayService();
}

GatewayProxy.prototype.getOnDemandContent = function(successCallback, failCallback)
{
    //this.gatewayService.getOnDemandContent(successCallback, failCallback);
}
